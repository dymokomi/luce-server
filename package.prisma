#prisma 4.0
def package "luce-server" {
    str owner = "dymokomi"
    str version = "0.1.2"
    str kind = "package"
    str language = "luce-base"
    str description = "A multithreaded server library written entirely in Luce Base. It provides HTTP routing, REST responses, static content, bounded file transfers, WebSocket sessions and TCP streams."
    str readme = "README.md"

    def export "http" {
        str module = "luce_server.http"
    }

    def export "websocket" {
        str module = "luce_server.websocket"
    }

    def export "socket" {
        str module = "luce_server.socket"
    }

    def dependency "luce-json" {
        str owner = "dymokomi"
        str version = "^0.2.0"
        str path = "../luce-json"
    }
}
