## JSON integers within Luce's range stay integers. Other numbers retain their
## exact validated spelling, avoiding silent float rounding in request data.
pub enum Value:
    null
    boolean(value: bool)
    integer(value: int)
    decimal(text: str)
    text(value: str)
    array(items: list[Value])
    object(fields: map[str, Value])
